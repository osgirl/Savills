import {
    StyleSheet,
    Platform
} from 'react-native';

const style = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        // justifyContent: 'space-between',
        // backgroundColor: '#F6F8FD'
    },
    viewBottom: {
        position: 'absolute',
        bottom: 0
    }
})

export default style;