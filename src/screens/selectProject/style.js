import {
    StyleSheet,
    Platform
} from 'react-native';
import resolution from '../../utils/resolution';

const style = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
    },
    viewBottom: {
        position: 'absolute',
        bottom: 0,
    }
})

export default style;